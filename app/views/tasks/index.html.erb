
<div class="container-fluid padding justify-content-center" id="loggin-top">
  <p id="notice"><%= notice %></p>
  <div class="col-sm-6 col-md-8 col-lg-12 info-tag">
    <span class="task-index justify-content-center">
      <h1 class="text-center"><%= current_user.name%> Tasks</h1>
      <% if current_user.role == "teacher" %>
        <%= link_to new_task_path, class: 'order-2 pr-2 bd-highlight' do %>
          <i class="fas fa-plus"> New Tvoz</i> |
        <% end %> 
      <% end%>  
      <%= link_to profile_user_path(current_user.id) do %>   
        <i class="fa-solid fa-user">Profile</i>
      <% end %>
    </span> 
  </div>
</div>
<div class="container-fluid padding" id="loggin-top">
  <div class="row justify-content-center">
    <%= paginate @tasks %>
    <div class="col-lg-12" id="loggin-inner-top">
      <div class="justify-content-center">
        <h5><%= paginate @tasks %></h5>
      </div>
      <div class="table-responsive"> 
        <table class="table">
          <thead class="table-light">
            <tr>
              <th>Title</th>
              <th>Image</th>
              <th>Created by</th>
              <th>Task Id</th>
              <th colspan="3">Homework</th>
            </tr>
          </thead>
          <tbody>
            <% @tasks.each do |task| %>
              <tr>
                <td><%= task.title %></td>
                <% if task.image.variable? %>
                  <td><%= image_tag(task.image, width: 70) %></td>
                <% else %>
                  <td> no image </td>
                <% end %> 
                <td><%= task.user.name %></td> 
                <td><%= task.id %></td> 
                <% if current_user.role == "teacher" %>
                  <td><%= link_to 'Show', task %></td>
                  <td><%= link_to 'Edit', edit_task_path(task) %></td>
                  <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                <% else %>
                  <td><%= link_to 'Tvoz', task %></td>
                <% end %>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>


 
 
