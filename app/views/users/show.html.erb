<h1>Users#teachers</h1>


<table class="table">
  <tbody>
    <%# <% @users.each do |user| %> 
    <tr class="user_management">
      <%# <th class= "text-justify">  %>
        <%# <div class=""> %>
          <%# <%= image_tag set_avatar, class: 'avatar-image-top'%> 
        <%# </div> %>
          <%# <%= image_tag set_avatar, class: 'avatar-image'%> 
      <%# </th> %>
      <th class= "text-justify">
        <p><%= @user.name %></p>
        <p><%= @user.email %></p>
        <p><%= @user.role %></p>
        <p><%= @user.admin? ? 'Admin' : 'none' %></p>
      </th>
      <th class= "text-justify">
        <%= link_to "Edit", edit_user_registration_path(@user.id), class: "btn btn-outline-success edit-btn" %>
        <%= link_to "Delete", user_registration_path(@user.id), method: :delete, data: {confirm: "User「#{@user.name}」will be delete, Are you sure?'"}, id: "delete_button", class: "btn btn-outline-danger delete-btn" %>
      </th>
    </tr>
    <%# <% end %> 
  </tbody>
</table>

<%# ONLY TEACHERS %>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Content</th>
      <th>Image</th>
      <th>Audio record</th>
      <th>Audio</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <th><%= task.title %></th>
        <th><%= task.content %></th>
        <th><%= task.image %></th>
        <th><%= task.audio_record %></th>
        <th><%= task.audio %></th>
        <th><%= task.user_id %></th>
        <th><%= link_to 'Show', task %></th>
        <th><%= link_to 'Edit', edit_task_path(task) %></th>
        <th><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></th>
      </tr>

    <% end %>
  </tbody>
</table>


<br>

<%= link_to 'New Task', new_task_path %> |
<%= link_to 'Home', root_path %>
